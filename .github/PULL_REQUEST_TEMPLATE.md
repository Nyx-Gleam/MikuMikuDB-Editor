## What does this PR do?

<!-- A short description of the change. -->

## Target branch check

<!-- To mark with a check fill the space in between "[ ]" with and "x"-->

- [ ] I'm targeting **`V2.0-dev`** (ongoing 2.0 work) — do NOT target `main` directly, it's the latest stable release only.
- [ ] If this is a hotfix for the stable release instead, I've mentioned that explicitly above.

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Translation / localization update
- [ ] Documentation only
- [ ] Refactor / internal change (no behavior change)

## How was this tested?

<!-- e.g. "Ran from source on Windows 11, created a pack with 3 songs, generated mod_pv_db.txt and verified it opens in-game." -->

## Checklist

- [ ] I ran the app from source and confirmed it still starts without errors.
- [ ] I didn't commit `config.toml`, `Autosaves/`, `.pdpack` files, `venv/`, or `__pycache__/` (see `.gitignore`).
- [ ] If I added a new dependency, I added it to `requirements.txt` (or explained why not, e.g. `madmom`).
- [ ] If I changed user-facing text, I flagged which language(s) still need updating in `data/lang.json`.

## Related issues

<!-- e.g. "Closes #12" -->
